## 1. Setup & Architecture

- [x] 1.1 Инициализировать проект Android (FigaTram) с Kotlin.
- [x] 1.2 Настроить зависимости в `build.gradle.kts` для Jetpack Compose, Google Maps, Retrofit, Room и WorkManager/Coroutines.
- [x] 1.3 Создать базовую структуру пакетов (ui, data, domain, di, service).
- [x] 1.4 Настроить базовую архитектуру MVVM (Model-View-ViewModel).
- [x] 1.5 Добавить файл `SecretConfig.kt` (с заглушками) и добавить его в `.gitignore`.

## 2. Data Layer & API Polling

- [x] 2.1 Создать data-классы для парсинга JSON ответа (массив `vehicles`, поля `vehicle_id`, `position.latitude`, `position.longitude`, `stop_id`).
- [x] 2.2 Настроить Retrofit2 + OkHttp3 (отключить SSL Pinning, добавить кастомные заголовки).
- [x] 2.3 Реализовать репозиторий для опроса API и получения **всех** трамваев на линии T3.

## 3. Database: Statistics & Directory

- [x] 3.1 Создать сущность Room `ArrivalStats` (id, vehicle_id, station_id, timestamp_arrived).
- [x] 3.2 Создать сущность Room `TramInfo` для справочника (vehicle_id, model, description, photo_uri, is_favorite).
- [x] 3.3 Создать DAO и Database класс для Room.
- [x] 3.4 Интегрировать логику записи события прибытия целевых (инклюзивных и любимых) трамваев в базу данных.

## 4. UI: Map & Tram Cards (Flightradar-style)

- [x] 4.1 Создать главный экран с Google Maps (используя `maps-compose`).
- [x] 4.2 Нарисовать на карте маршрут линии T3 (Polyline) и расставить маркеры всех остановок с их названиями (координаты можно захардкодить или вынести в отдельный JSON).
- [x] 4.3 Отобразить все трамваи на карте, при этом специально выделив инклюзивные трамваи (152, 175).
- [x] 4.4 Реализовать логику анимации плавного перемещения маркеров.
- [x] 4.5 Добавить UI карточки трамвая (`ModalBottomSheet`), открывающейся при клике на маркер. Карточка должна отображать данные из справочника (БД) и позволять прикрепить/сделать фото.

## 5. UI: Directory & Settings

- [x] 5.1 Создать экран "Справочник" для просмотра всех известных системе трамваев (с миниатюрами фото).
- [x] 5.2 Создать экран "Статистика" для отображения истории прибытий (152, 175), сгруппированной по дням/времени.
- [x] 5.3 Добавить экран настроек для выбора нескольких станций посадки и гибкого времени оповещения (за сколько минут до прибытия).
- [x] 5.4 Добавить UI-чекбокс "Любимый" в карточку трамвая в справочнике.

## 6. Background Monitoring & Notifications

- [x] 6.1 Реализовать Foreground Service для периодического поллинга API (каждые 20 сек).
- [x] 6.2 Реализовать алгоритм расчета ETA целевых трамваев (инклюзивных и "любимых") до всех выбранных пользователем станций.
- [x] 6.3 Интегрировать отправку Local Push Notification, когда расчетное ETA <= заданное время оповещения.
